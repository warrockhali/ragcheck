# Database

Database files will define persistence for RAGCheck database entities.

MVP entities:

- Project
- RagEndpoint
- TestCase
- EvaluationRun
- EvaluationResult

Use `migrations/` for schema migrations and `seeds/` for demo or development seed data.
