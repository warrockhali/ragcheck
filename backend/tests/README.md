# Backend Tests

Backend tests will cover externally visible behavior for API routes, services, evaluators, clients, models, and schemas.

Do not test chatbot behavior. RAGCheck evaluates external RAG APIs.
